# GCP MCP — Google Cloud Platform

# Cấu hình đầy đủ để dùng trong mcpServers:
# gcp:
#   command: npx
#   args: ["-y", "@google-cloud/mcp-server"]
#   transport: stdio
#   env:
#     GOOGLE_APPLICATION_CREDENTIALS: "/path/to/service-account.json"

## TODO: Thêm chi tiết khi cần dùng
