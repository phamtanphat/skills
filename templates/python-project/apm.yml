# apm.yml — Python Project Template
# Copy file này vào thư mục gốc của dự án Python/FastAPI

name: my-python-app            # ← Đổi tên dự án
version: 1.0.0

skills:
  # --- coding ---
  - name: python-review
    description: Quy chuẩn PEP8 và Python best practices
    path: ~/Desktop/skills/features/python/skills/code-review.md
    activate:
      include: ["**/*.py"]

  - name: fastapi-patterns
    description: FastAPI patterns chuẩn
    path: ~/Desktop/skills/features/python/skills/fastapi-patterns.md
    activate:
      include: ["**/*.py"]

  - name: git-workflow
    description: Git workflow chuẩn
    path: ~/Desktop/skills/features/workflow/skills/git-workflow.md
    activate:
      always: true

  # --- structure ---
  - name: clean-architecture
    description: Clean Architecture guide
    path: ~/Desktop/skills/features/structure/skills/architecture/clean-architecture.md
    activate:
      always: true

  # --- testing ---
  - name: unit-test-guide
    description: Hướng dẫn viết unit test với pytest
    path: ~/Desktop/skills/features/testing/skills/unit-test-guide.md
    activate:
      include: ["**/test_*.py", "**/*_test.py"]

  # --- delivery ---
  - name: docker-guide
    description: Docker containerization guide
    path: ~/Desktop/skills/features/delivery/skills/docker-guide.md
    activate:
      include: ["**/Dockerfile", "**/docker-compose*.yml"]

mcpServers:
  serena:
    command: uvx
    args:
      - "--from"
      - "git+https://github.com/oraios/serena"
      - "serena"
      - "start-mcp-server"
    transport: stdio

  context7:
    command: npx
    args: ["-y", "@upstash/context7-mcp"]
    transport: stdio
    env:
      CONTEXT7_API_KEY: "ctx7sk-31f118e9-1ca9-4dfc-8a59-4cd58ec0a630"

  # Bỏ comment nếu dùng Supabase
  # supabase:
  #   command: npx
  #   args: ["-y", "@supabase/mcp-server-supabase@latest"]
  #   transport: stdio
  #   env:
  #     SUPABASE_URL: "https://your-project.supabase.co"
  #     SUPABASE_KEY: "your-service-role-key"
